import React, {useState, useEffect} from 'react'

function DocTitle() {
    const [count, setCount] = useState(5)

    useEffect(() => {
        setTimeout(() => {
            
        }, timeout);
    })
  return (
    <div>

    </div>
  )
}

export default DocTitle